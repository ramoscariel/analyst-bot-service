# Application Configuration
APP_NAME=Analyst Bot Service
APP_VERSION=1.0.0
ENVIRONMENT=development

# SQL Server Configuration
DB_SERVER=localhost
DB_PORT=1433
DB_NAME=BusinessDB
DB_DRIVER=ODBC Driver 18 for SQL Server

# Database Authentication
# DB_AUTH_TYPE: "windows" | "sql" | "auto" (default: auto)
#   - windows: Use Windows Authentication (Trusted_Connection)
#   - sql: Use SQL Authentication (UID/PWD)
#   - auto: Detect based on server name (Azure SQL uses SQL auth, others use Windows)
DB_AUTH_TYPE=auto

# SQL Authentication credentials (required when DB_AUTH_TYPE=sql or connecting to Azure SQL)
DB_USER=your_username
DB_PASSWORD=your_secure_password

# Google Gemini API
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-pro

# LLM Global Context (optional)
LLM_GLOBAL_CONTEXT_PATH=llm_global_context.example.txt

# API Configuration
API_V1_PREFIX=/api/v1
MAX_QUERY_ROWS=10000
QUERY_TIMEOUT_SECONDS=30
